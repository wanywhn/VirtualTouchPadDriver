post_install() {
  cat<<EOF

Unload and load kernel modules:

  rmmod virtual_touchpad
  modprobe virtual_touchpad

EOF

  rmmod virtual_touchpad
  modprobe virtual_touchpad
}

post_upgrade() {
  post_install
}
